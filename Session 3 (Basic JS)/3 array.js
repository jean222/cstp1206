// Arrays are used to store collection of data

let sutentsList = []; //There is no data inside, which means it's an empty array

let students = ["Kevin" , "David"]; //Array with string 
let list = ["Kevin", 30, true]; //Array with multiple 

list [0]; "Kevin"
list [10]; //undefined

list[3] = "David";

console.log(list.length); //4

// pUsh Method (Adds element in the last index of the array)

let fruitsList = ["Apple", "Mangoes"];

fruitsList.push("Pineapple", "Orange", "Kiwi");


//************************* POP Method (Removes element from thelast indx ofthe array)*/

fruitsList.pop();
console.log(fruitsList, "after pop");

//************************************* Shift Method */